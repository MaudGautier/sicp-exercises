;Explicit dispatch:
;Change every function when a new type is added (add the new type to each)

;Data-directed style:
;Install a new package when a new type is added (upper-level calls don't change)

;Message-passing style:
;Add the new type of object that accepts the same operations
